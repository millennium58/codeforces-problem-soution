#include<stdio.h>
int main()
{
  char s[100];
  int i,l,n;
  scnaf("%d",&n);
  for(i=1;i<=n;i++)
  {
      scanf("%s",&s);
      l==strlen(s);
      if(l<=10)
        printf("%s",s);
  }
  return 0;
}
